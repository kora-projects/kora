name: Publish Release

on:
  release:
    branches: [ "master" ]
    types: [ published ]

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: JDK 25 setup
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'

      - name: Print release version
        run: echo "Release version - $KORA_VERSION"
        env:
          KORA_VERSION: ${{ github.event.release.tag_name }}

      - name: Gradle setup
        uses: gradle/actions/setup-gradle@v4

      - name: Gradle artifact
        run: ./gradlew assemble jar --no-daemon --parallel

      - name: Gradle publish
        run: ./gradlew uploadPublishArchive -Psigning.secretKeyRingFile=/tmp/secring.gpg --no-daemon --parallel
        env:
          KORA_IO_SIGNING_KEY: ${{ secrets.KORA_IO_SIGNING_KEY }}
          KORA_IO_SIGNING_PASSWORD: ${{ secrets.KORA_IO_SIGNING_PASSWORD }}
          SONATYPE_KORA_IO_USERNAME: ${{ secrets.SONATYPE_KORA_IO_USERNAME }}
          SONATYPE_KORA_IO_PASSWORD: ${{ secrets.SONATYPE_KORA_IO_PASSWORD }}
          KORA_VERSION: ${{ github.event.release.tag_name }}

  notify:
    runs-on: ubuntu-latest
    needs: publish
    timeout-minutes: 10
    steps:
      - id: kora_changelog
        run: |
          KORA_CHANGELOG_TAG=${{ github.event.release.tag_name }}
          KORA_CHANGELOG_TAG=${KORA_CHANGELOG_TAG//.}
          echo "KORA_CHANGELOG_TAG=${KORA_CHANGELOG_TAG}" >> $GITHUB_OUTPUT

      - name: Extract description
        run: |
          echo "KORA_CHANGELOG_DESCRIPTION<<EOF" >> $GITHUB_ENV
          echo "Часть изменений в $(curl -s https://kora-projects.github.io/kora-docs/ru/changelog/changelog/ | grep -o '<meta property="og:description" content=".*' | grep -o '"[^"]*"' | tail -n +2 | tr -d '"' | sed 's/[:,]/\n-/g') " >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

# Temporary disabled before stable release 2.0
#      - name: Notify new release to Telegram
#        uses: appleboy/telegram-action@221e6b684967abe813051ee4a37dd61770a83ad3 # v1.0.1
#        env:
#          KORA_VERSION: ${{ github.event.release.tag_name }}
#          KORA_CHANGELOG_TAG: ${{ steps.kora_changelog.outputs.KORA_CHANGELOG_TAG }}
#        with:
#          to: ${{ secrets.TELEGRAM_RELEASE_CHAT_ID }}
#          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
#          format: markdown
#          message: |
#            Вышла новая версия Kora - ${{ env.KORA_VERSION }}
#
#            ${{ env.KORA_CHANGELOG_DESCRIPTION }}
#
#            Полный список в [журнале изменений](https://kora-projects.github.io/kora-docs/ru/changelog/changelog/#${{ env.KORA_CHANGELOG_TAG }})
