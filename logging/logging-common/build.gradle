dependencies {
    annotationProcessor project(':config:config-annotation-processor')
    annotationProcessor libs.graalvm.processor
    compileOnly libs.graalvm.annotations

    api project(':common')
    api project(':json:json-common')
    api project(':config:config-common')
    api libs.slf4j.api
    api libs.slf4j.jul
}
