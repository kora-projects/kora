# Apache HTTP Client configuration example
httpClient {
  connectTimeout = 5s
  readTimeout = 30s
  followRedirects = true
  maxRedirects = 5
  connectionStateEnabled = true
  userAgent = "Kora-Apache-HttpClient/1.0"
}