apply plugin: 'org.jetbrains.kotlin.jvm'

kotlin {
    jvmToolchain(25)
}

tasks.matching { it.class.name.startsWith("org.jetbrains.kotlin.gradle.tasks.KotlinCompile") }.configureEach {
    compilerOptions {
        javaParameters = true
    }
}
